export function Header() {
  return (
    <header className="border-b border-agent bg-agent-soft px-4 py-3">
      <div>
        <h1 className="m-0 text-[15px] font-semibold">Coding Agent</h1>
        <p className="mt-1 text-xs leading-5 text-muted">Agent runtime with sessions, tools, permissions, and provider adapters.</p>
      </div>
    </header>
  );
}
